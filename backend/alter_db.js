require('dotenv').config(); const { pool } = require('./src/config/db'); async function run() { try { await pool.query('ALTER TABLE services ADD COLUMN IF NOT EXISTS price NUMERIC(12,2) DEFAULT 0'); await pool.query('ALTER TABLE services ADD COLUMN IF NOT EXISTS icon VARCHAR(50)'); await pool.query('ALTER TABLE services ALTER COLUMN category_id DROP NOT NULL'); console.log('DB altered'); } catch(e) { console.error(e); } finally { process.exit(); } } run();
